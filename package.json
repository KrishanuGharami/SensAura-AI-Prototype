{
  "name": "sensaura-ai",
  "version": "1.0.0",
  "description": "SensAura AI - Offline-First, On-Device Contextual Automated Home Assistant",
  "private": true,
  "scripts": {
    "build": "npm run vercel-build",
    "vercel-build": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable flutter; fi && ./flutter/bin/flutter build web --release"
  }
}
